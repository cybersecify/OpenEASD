from django.apps import AppConfig


class DNSAnalyzerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.dns_analyzer"
    label = "dns_analyzer"
    verbose_name = "DNS Analyzer"
