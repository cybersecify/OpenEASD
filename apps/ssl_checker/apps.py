from django.apps import AppConfig


class SSLCheckerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ssl_checker"
    label = "ssl_checker"
    verbose_name = "SSL Checker"
