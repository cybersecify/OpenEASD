apiVersion: v1
kind: Service
metadata:
  name: openeasd
  namespace: openeasd
  labels:
    app: openeasd
spec:
  selector:
    app: openeasd
    component: web
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
